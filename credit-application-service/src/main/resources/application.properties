# Application
spring.application.name=credit-application-service

# Datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/creditdb
spring.datasource.username=credituser
spring.datasource.password=creditpass
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# JWT Settings
# Clave secreta en Base64 (ejemplo generado con openssl: `openssl rand -base64 32`)
application.security.jwt.secret-key=VGhpcyBpcyBhIHZhbGlkIHNlY3JldCBleGFtcGxlIGtleQ==
application.security.jwt.expiration=86400000
